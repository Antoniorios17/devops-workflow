Devops Workflow 

This repository contains a GitHub Actions workflow to build and push a Docker image to DockerHub whenever there is a push or pull request to the main branch. The workflow is defined in two files: `deployment.yml` and `build_and_push_to_dockerhub.yml`.